# Enable mouse support for scrolling, selecting, and resizing panes
set -g mouse on

# Enable vi mode for copy mode
setw -g mode-keys vi

# Set scrollback buffer size
set -g history-limit 10000

# Enable copy to system clipboard
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'
bind -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# Enable paste from system clipboard
bind p run "xclip -out -selection clipboard | tmux load-buffer - ; tmux paste-buffer"

# Make scrolling work like in normal terminal
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M

# Set terminal type
set -g default-terminal "screen-256color"

# Enable focus events for terminal applications
set -g focus-events on

# Reduce escape delay for better responsiveness
set -sg escape-time 10

# Enable automatic window renaming
setw -g automatic-rename on

# Start window and pane numbering from 1
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Enable status bar
set -g status off
set -g status-interval 1

# Dim inactive panes
set -g window-active-style 'fg=colour226,bg=black,bold'        # normal active pane
set -g window-style 'fg=colour8,bg=black'               # grey/dim foreground

set-option -g pane-active-border-style "fg=orange,bg=black,bold"
set-option -g pane-border-style "fg=black,bg=black"
set-option -g pane-border-lines double
set-option -g pane-border-indicators arrows
set-option -g pane-border-status top